% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm.R
\name{textmodel_gmm}
\alias{textmodel_gmm}
\title{Topic analysis using Gaussian mixture models}
\usage{
textmodel_gmm(
  x,
  k = 10,
  model = NULL,
  seeds = NULL,
  ...,
  verbose = quanteda_options("verbose")
)
}
\arguments{
\item{x}{a \link[wordvector:textmodel_doc2vec]{wordvector::textmodel_doc2vec} or a dense matrix of document vectors in the rows.}

\item{k}{the number of topics to identify.}

\item{model}{a fitted model from which initial centroids are extracted.}

\item{seeds}{a matrix created using \link{as.seedwords}.}

\item{...}{passed to the underlying function.}

\item{verbose}{print the progress if \code{TRUE}.}

\item{omit}{indices of singular values of \code{x} to be zero.}
}
\value{
Returns a fitted \code{textmodel_gmm} object.
}
\description{
Perform topic analysis of document vectors using Gaussian mixture models.
}
\details{
Users can change the number of threads for the parallel computing via
\code{options(GMTM.threads)} or \code{OMP_THREAD_LIMIT} in the environmental
variable.

\code{omit} is used to reduce the noise in the \code{x} by applying \code{base::svd} before
applying GMM. Singular values corresponding to \code{omit} are set to
zero, removing their variance in \code{x}. See Chan et al. (2020)
\url{doi:10.1080/19312458.2020.1812555} for the methodology.

The number of iterations in k-means (\code{iter_km}) and expectation maximization
(\code{iter_em}) stages can be set via \code{...}.
}
\examples{
library(quanteda)
library(wordvector)
options(wordvector_threads = 2)

corp <- head(wordvector::data_corpus_news2014, 1000)
toks <- tokens(corp, remove_punct = TRUE,
               remove_symbols = TRUE, remove_numbers = TRUE) \%>\%
        tokens_remove(stopwords("en"), min_nchar = 2)
wov <- textmodel_word2vec(toks, dim = 50)
dov <- as.textmodel_doc2vec(dfm(toks), wov)

gmm <- textmodel_gmm(dov, k = 10)
table(topics(gmm))
}
