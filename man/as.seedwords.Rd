% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{as.seedwords}
\alias{as.seedwords}
\title{Convert a dictionary to a seed word matrix}
\usage{
as.seedwords(x, model, residual = 0, levels = 1)
}
\arguments{
\item{x}{a \link[quanteda:dictionary]{quanteda::dictionary} of seed words.}

\item{model}{a \link[wordvector:textmodel_word2vec]{wordvector::textmodel_word2vec} object.}

\item{residual}{the number of unseeded topics.}

\item{levels}{integers specifying the levels of entries in
a hierarchical dictionary.}
}
\value{
Returns a seed word matrix.
}
\description{
Convert a dictionary to a seed word matrix
}
\details{
Unseeded topics are labeled "other", but it can be changed via
options("GMTM.residual.name").
}
\examples{
library(quanteda)
library(wordvector)
options(wordvector_threads = 2)

corp <- head(wordvector::data_corpus_news2014, 1000)
toks <- tokens(corp, remove_punct = TRUE,
               remove_symbols = TRUE, remove_number = TRUE) \%>\%
        tokens_remove(stopwords("en"), min_nchar = 2)
wov <- textmodel_word2vec(toks, dim = 50)

dict <- dictionary(list(eco = "econom*", sec = "securit*", spo = "sport*",
                        pol = "politi*", cri = "crime"))
seed <- as.seedwords(dict, wov, residual = 2)
}
